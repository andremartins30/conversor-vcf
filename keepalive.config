# Configuração do Keep Alive
# URL da aplicação Railway

# URL da aplicação (Railway)
APP_URL="https://vcf-converter.up.railway.app"

# Frequência (em minutos) - para Railway recomenda-se 4 minutos
FREQUENCY_MINUTES=4

# Arquivo de log
LOG_FILE="$HOME/keepalive.log"

# Timeout para requisições (segundos)
TIMEOUT=30
